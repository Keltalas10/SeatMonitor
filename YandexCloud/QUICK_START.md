# Быстрый старт: Интеграция Chrome Extension с Yandex Cloud

## 🎯 Цель
Подключить Chrome Extension к Yandex Cloud Functions через API Gateway.

## ⏱️ Время выполнения: ~15-20 минут

---

## Шаг 1: Получить ID функций и Service Account (5 мин)

### 1.1. Получить ID функции `registerUser`:
```bash
yc serverless function get registerUser --format json | grep -oP '"id": "\K[^"]*'
```
Или через веб-интерфейс:
- Yandex Cloud Console → Functions → `registerUser` → скопировать ID из URL или раздела "Общая информация"

### 1.2. Получить ID функции `loginUser`:
```bash
yc serverless function get loginUser --format json | grep -oP '"id": "\K[^"]*'
```

### 1.3. Получить Service Account ID:
```bash
yc iam service-account list --format json | grep -oP '"id": "\K[^"]*'
```
Или через веб-интерфейс:
- Yandex Cloud Console → IAM → Сервисные аккаунты → найти аккаунт, используемый функциями → скопировать ID

---

## Шаг 2: Настроить API Gateway (5 мин)

### 2.1. Отредактировать спецификацию:
Откройте `YandexCloud/seat-monitor-functions/api-gateway-spec.yaml` и замените:
- `<FUNCTION_ID_REGISTER>` на ID функции `registerUser`
- `<FUNCTION_ID_LOGIN>` на ID функции `loginUser`
- `<SERVICE_ACCOUNT_ID>` на ID сервисного аккаунта

### 2.2. Создать API Gateway:
```bash
cd YandexCloud/seat-monitor-functions
yc serverless api-gateway create \
  --name seat-monitor-api \
  --description "API для Seat Monitor Extension" \
  --spec api-gateway-spec.yaml
```

### 2.3. Получить URL API Gateway:
```bash
yc serverless api-gateway get seat-monitor-api --format json | grep -oP '"domain": "\K[^"]*'
```
Или через веб-интерфейс:
- Yandex Cloud Console → API Gateway → `seat-monitor-api` → скопировать Domain

**Сохраните этот URL!** Он понадобится в следующем шаге.

---

## Шаг 3: Обновить Chrome Extension (3 мин)

### 3.1. Обновить `auth.js`:
1. Откройте `ChromeExtension/js/auth.js`
2. Найдите строку: `apiUrl: null,`
3. Замените на: `apiUrl: 'https://YOUR-API-GATEWAY-DOMAIN',` (используйте URL из шага 2.3)

Или скопируйте готовый файл:
```bash
# Windows PowerShell
Copy-Item ChromeExtension/js/auth.js.example ChromeExtension/js/auth.js -Force
# Затем отредактируйте auth.js и замените YOUR-API-GATEWAY-URL на реальный URL
```

### 3.2. Активировать интеграцию:
В `auth.js` раскомментируйте код в методах `register()` и `login()` (строки 58-69 и 113-124), или используйте готовый файл `auth.js.example` как основу.

---

## Шаг 4: Протестировать (5 мин)

### 4.1. Тест API Gateway:
```bash
# Тест регистрации
curl -X POST https://YOUR-API-GATEWAY-DOMAIN/register \
  -H "Content-Type: application/json" \
  -d '{"deviceUUID":"123e4567-e89b-12d3-a456-426614174000"}'

# Ожидаемый ответ:
# {"success":true,"deviceUUID":"123e4567-e89b-12d3-a456-426614174000",...}

# Тест входа
curl -X POST https://YOUR-API-GATEWAY-DOMAIN/login \
  -H "Content-Type: application/json" \
  -d '{"deviceUUID":"123e4567-e89b-12d3-a456-426614174000"}'
```

### 4.2. Тест Chrome Extension:
1. Загрузите расширение в Chrome (режим разработчика)
2. Откройте любую веб-страницу
3. Откройте DevTools (F12) → Console
4. Проверьте логи:
   - `[Auth] Сгенерирован новый UUID устройства: ...`
   - `[Auth] Устройство успешно зарегистрировано через API: ...`
5. Откройте popup расширения
6. Проверьте отображение ID устройства и даты подписки

### 4.3. Проверка базы данных:
1. Yandex Cloud Console → YDB → ваша база данных
2. Выполните запрос:
```sql
SELECT * FROM users;
```
3. Убедитесь, что запись создана

---

## ✅ Готово!

Если все шаги выполнены успешно:
- ✅ API Gateway создан и работает
- ✅ Chrome Extension подключен к API
- ✅ Регистрация и вход работают через Yandex Cloud
- ✅ Данные сохраняются в YDB
- ✅ Popup отображает информацию об устройстве

---

## 🔧 Если что-то не работает

### Ошибка: "Failed to fetch"
- Проверьте URL API Gateway в `auth.js`
- Убедитесь, что API Gateway создан и активен
- Проверьте логи функции в Yandex Cloud Console

### Ошибка: CORS
- API Gateway автоматически обрабатывает CORS
- Убедитесь, что функции возвращают правильные заголовки (уже настроено)

### Ошибка: "Function not found"
- Проверьте ID функций в `api-gateway-spec.yaml`
- Убедитесь, что функции существуют и имеют версию

### Данные не сохраняются в БД
- Проверьте переменные окружения функций (`YDB_ENDPOINT`, `YDB_DATABASE`)
- Проверьте права доступа сервисного аккаунта (`ydb.editor`)
- Проверьте логи функции в Yandex Cloud Console

---

## 📚 Дополнительная информация

- Подробная инструкция: `INTEGRATION_PLAN.md`
- Инструкция по деплою функций: `seat-monitor-functions/DEPLOY.md`
- Спецификация API Gateway: `seat-monitor-functions/api-gateway-spec.yaml`

